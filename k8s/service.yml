apiVersion: v1
kind: Service
metadata:
  name: cookie-factory-service
spec:
  type: NodePort
  selector:
    app: cookie-factory
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080  # Доступ по http://localhost:30080
